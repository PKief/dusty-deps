{
  "name": "dusty-deps",
  "version": "0.1.0",
  "description": "Check the age of npm dependencies and flag stale packages",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "bin": {
    "dusty-deps": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "lint:fix": "biome check --fix .",
    "format": "biome format --write .",
    "check": "biome check . && tsc --noEmit",
    "prepare": "husky",
    "prepublishOnly": "npm run build"
  },
  "lint-staged": {
    "*.{ts,js,json}": "biome check --fix --no-errors-on-unmatched"
  },
  "keywords": [
    "npm",
    "dependencies",
    "stale",
    "outdated",
    "age",
    "maintenance"
  ],
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@types/node": "^22.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.3.3",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
